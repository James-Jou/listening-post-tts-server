# 這個 repo 的檔案跟遊戲專案的 tools/kokoro_server/ 是硬連結（同一份實體檔案），
# 所以 git 絕對不可以改寫工作區的內容——換行尾也算改寫，會產生新的 inode，
# 硬連結就斷了，而且不會有任何錯誤訊息。
#
# `-text` 的意思是「照位元組原樣存、原樣取出」，關掉所有換行尾轉換。
# 這也順便解決 TTS_Server.spec 一直顯示為已修改的問題：那個檔案由 PyInstaller
# 在 Windows 上產生，用的是 CRLF，而 git 當初收進去的是 LF。
* -text
