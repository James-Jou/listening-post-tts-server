# 版本全部釘死，這是為了符合 GPLv3 §6 的要求。
#
# §6 要交付的是「對應原始碼」，也就是照著它可以重建出實際散布那支執行檔的原始碼。
# 如果這裡寫 >=，別人照著建出來的會是另一組相依組合，那就不算對應。
#
# 改版本的時候：修改本檔 → 重跑 build.ps1 → 重新收集一次相依套件的授權檔。
# 如果動到的是 espeakng-loader 或 phonemizer-fork，散布端隨附的對應原始碼壓縮檔
# 也要跟著換，否則交出去的原始碼會跟實際散布的執行檔對不上。

kokoro-onnx==0.5.0
onnxruntime==1.23.2
numpy==2.2.6
pyinstaller==6.21.0

# 下面兩個原本是 kokoro-onnx 的間接相依，這裡明寫出來，因為它們是 GPL-3.0 的元件，
# 版本必須看得到、查得到，不能隨相依解析的結果浮動。
#
# espeakng-loader 隨附的 espeak-ng.dll 是用 espeak-ng 1.52.0 建的
# （espeakng-loader 0.2.4 的建置流程以 `-b 1.52.0` 取得上游原始碼）。
# 散布這支程式的時候，那個版本的原始碼也要一併交付。
espeakng-loader==0.2.4
phonemizer-fork==3.3.2
